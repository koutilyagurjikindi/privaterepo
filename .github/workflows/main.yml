name: Demo
on: push
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Git checkout
        uses: actions/checkout@v2
      - name: Echo myname
        run: |
          echo "Hello world"
